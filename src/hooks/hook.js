export class Hook {
    /**
     * @param {Sqomplexity.Expression} expr
     * @param {string} clause
     * @param {Calculator} self
     * @returns {void}
     */
    handle(expr, clause, self) {
        //
    }

    /**
     * @returns {object}
     */
    stats() {
        return {
            //
        };
    }
}